#include <stdio.h>

int main() {
    /* Pada file ini, kalian diminta untuk menjawab tiap soal */
    /* dengan menampilkan hasilnya pada layar */

    /* Contoh */
    printf("0. Sebutkan nama lengkap dari Kak Nigel!\n");
    printf("Jawaban: Nigel Sahl\n");

    /* Nomor 1 [5 point] */
    printf("1. Sebutkan jenis-jenis primitive data type di C!\n");
    printf("Jawaban:\n");
    printf("char (karakter), int (bilangan bulat), float (desimal), double (bilangan desimal lebih presisi), short (integer pendek), long (integer panjang), unsigned (tanpa tanda), signed (dengan tanda)\n"); // lengkapi di sini. jawaban tidak dibatasi dalam 1 baris

    /* Nomor 2 [5 point] */
    printf("2. Sebutkan perbedaan fungsi dan prosedur!\n");
    printf("Jawaban:\n");
    printf("Perbedaan antara fungsi dan prosedur adalah bahwa fungsi memiliki nilai kembalian sementara prosedur tidak memiliki nilai kembalian. Fungsi dipanggil untuk menghitung atau mengubah nilai, sementara prosedur dipanggil untuk melakukan tugas tertentu. Fungsi dapat digunakan sebagai bagian dari ekspresi dan dapat ditugaskan ke variabel, sementara prosedur hanya dapat dipanggil sebagai bagian dari pernyataan.\n"); // lengkapi di sini. jawaban tidak dibatasi dalam 1 baris

    /* Nomor 3 [5 point] */
    int **val;
    printf("3. Apa tipe data dari deklarasi variabel di atas?");
    printf("Jawaban:\n");
    printf("Pointer integer\n");

    return 0;
}
